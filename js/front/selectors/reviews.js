// @flow
import type {State as $IMPORTED_TYPE$_State} from "../reducer/reviews";import type { State } from 'front/reducer';

export const getReviews = (state: State): $IMPORTED_TYPE$_State => state.reviews;

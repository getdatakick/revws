// @flow
import type {State as $IMPORTED_TYPE$_State} from "../reducer/settings";import type { State } from 'back/reducer';

export const getSettings = (state: State): $IMPORTED_TYPE$_State => state.settings;
